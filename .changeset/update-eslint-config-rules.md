---
'@nelsonlaidev/eslint-config': patch
---

Update custom rule references to match eslint-plugin changes

- Replace `lucide-prefer-loader-icon` and `lucide-prefer-trash2-icon` with `lucide-restrict-import`
