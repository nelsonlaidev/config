---
'@nelsonlaivo/eslint-config': minor
---

Add `eslint-plugin-promise` to enforce best practices for JavaScript promises, including rules for always returning values, avoiding nested promises, and preferring async/await over .then().catch() chains.
