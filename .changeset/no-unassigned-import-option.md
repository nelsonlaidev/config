---
'@nelsonlaidev/eslint-config': minor
---

Add `noUnassignedImport` option to `import-x` config to allow custom glob patterns in the `no-unassigned-import` rule. This follows the same pattern as the existing `noNamespace` option, enabling users to extend the default allowed patterns for unassigned imports.
