---
'@nelsonlaidev/eslint-config': minor
'@nelsonlaidev/oxlint-config': minor
---

Stop injecting default and custom `better-tailwindcss` selectors in shared configs. Consumers can now provide `tailwindcss.selectors` explicitly when they need custom class detection.
