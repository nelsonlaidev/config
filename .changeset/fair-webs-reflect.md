---
'@nelsonlaidev/eslint-config': minor
'@nelsonlaidev/oxlint-config': minor
---

Convert all `warn` rule severities to `error` for built-in presets, add `makeAllErrors` to base layer (not affecting user-provided configs/overrides), handle array-form rule options and numeric severity.
