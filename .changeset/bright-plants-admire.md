---
'@nelsonlaidev/oxlint-config': patch
---

Document that the Oxlint config is synced from `eslint-config`, enables matching supported Oxlint rules, and intentionally excludes `unused-imports` because Oxlint `jsPlugins` does not currently support the required type information.
