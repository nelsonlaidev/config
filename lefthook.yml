pre-commit:
  jobs:
    - name: oxfmt
      run: pnpm oxfmt "{staged_files}"
      stage_fixed: true

    - name: lint
      glob: '*.{js,jsx,cjs,mjs,ts,tsx,cts,mts,d.ts,d.cjs,d.mts}'
      run: pnpm oxlint --fix --max-warnings 0 "{staged_files}"
      stage_fixed: true
